name: flutter_top_snackbar
description: "A Flutter package to display customizable snackbars from the top of the screen with themes, animations, and custom icons."
version: 1.0.2
homepage: https://github.com/AbishekAB21/flutter_top_snackbar

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true