name: flutter_top_snackbar
description: "A customizable top snackbar widget with animations and themes."
version: 0.0.1
homepage: https://github.com/AbishekAB21/flutter_top_snackbar

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true